diff -ru '--exclude=__pycache__' /tmp/stock_sglang/sglang/srt/mem_cache/pool_host/mamba.py /data/models/mtp_experiment_0907/sglang/srt/mem_cache/pool_host/mamba.py
--- /tmp/stock_sglang/sglang/srt/mem_cache/pool_host/mamba.py	2026-08-29 22:18:23.000000000 +0800
+++ /data/models/mtp_experiment_0907/sglang/srt/mem_cache/pool_host/mamba.py	2026-09-09 10:42:05.008322567 +0800
@@ -18,7 +18,7 @@
     ALLOC_MEMORY_FUNCS,
     get_allocator_from_storage,
 )
-from sglang.srt.utils import is_cuda, is_hip
+from sglang.srt.utils import is_cuda, is_hip, is_npu
 
 _is_cuda = is_cuda()
 _is_hip = is_hip()
@@ -29,6 +29,12 @@
         transfer_kv_per_layer_direct_pf_lf,
         transfer_kv_per_layer_mla,
     )
+if is_npu():
+    from sglang.srt.mem_cache.pool_host.npu_direct_compat import (
+        transfer_kv_all_layer_direct_lf_pf,
+        transfer_kv_direct,
+        transfer_kv_per_layer_direct_pf_lf,
+    )
 if _is_cuda or _is_hip:
     from sglang.kernels.ops.mamba.transfer_mamba import (
         transfer_kv_mamba_lf_pf,
@@ -313,7 +319,7 @@
                 dst_indices=dst_indices,
                 item_size=MambaPoolHost._item_size_per_index(src),
             )
-        elif io_backend == "direct":
+        elif io_backend in ("direct", "kernel_ascend"):
             transfer_kv_direct(
                 src_layers=[src],
                 dst_layers=[dst],
@@ -352,7 +358,7 @@
                 item_size=item_size,
                 src_layout_dim=item_size * num_layers,
             )
-        elif io_backend == "direct":
+        elif io_backend in ("direct", "kernel_ascend"):
             transfer_kv_per_layer_direct_pf_lf(
                 src_ptrs=[src],
                 dst_ptrs=[dst],
@@ -395,7 +401,7 @@
                 dst_layout_dim=item_size * num_layers,
                 num_layers=num_layers,
             )
-        elif io_backend == "direct":
+        elif io_backend in ("direct", "kernel_ascend"):
             src_ptrs = [src_layers[i] for i in range(num_layers)]
             transfer_kv_all_layer_direct_lf_pf(
                 src_ptrs=src_ptrs,
