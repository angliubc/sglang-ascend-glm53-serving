diff -ru '--exclude=__pycache__' /tmp/stock_sglang/sglang/srt/mem_cache/pool_host/dsa.py /data/models/mtp_experiment_0907/sglang/srt/mem_cache/pool_host/dsa.py
--- /tmp/stock_sglang/sglang/srt/mem_cache/pool_host/dsa.py	2026-08-29 22:18:23.000000000 +0800
+++ /data/models/mtp_experiment_0907/sglang/srt/mem_cache/pool_host/dsa.py	2026-09-09 10:29:27.243112034 +0800
@@ -42,6 +42,12 @@
         transfer_kv_per_layer_mla,
         transfer_kv_per_layer_mla_pf_lf,
     )
+if _is_npu:
+    from sglang.srt.mem_cache.pool_host.npu_direct_compat import (
+        transfer_kv_all_layer_direct_lf_pf,
+        transfer_kv_direct,
+        transfer_kv_per_layer_direct_pf_lf,
+    )
 
 logger = logging.getLogger(__name__)
 
@@ -259,7 +265,7 @@
                 )
             else:
                 raise ValueError(f"Unsupported layout: {self.layout}")
-        elif io_backend == "direct":
+        elif io_backend in ("direct", "kernel_ascend"):
             if self.layout == "layer_first":
                 transfer_kv_direct(
                     src_layers=[self.index_k_with_scale_buffer[host_layer_id]],
@@ -316,7 +322,7 @@
                 )
             else:
                 raise ValueError(f"Unsupported layout: {self.layout}")
-        elif io_backend == "direct":
+        elif io_backend in ("direct", "kernel_ascend"):
             if self.layout == "layer_first":
                 transfer_kv_direct(
                     src_layers=[device_pool.index_k_with_scale_buffer[device_layer_id]],
@@ -391,7 +397,7 @@
                     )
             else:
                 raise ValueError(f"Unsupported layout: {self.layout}")
-        elif io_backend == "direct":
+        elif io_backend in ("direct", "kernel_ascend"):
             if self.layout == "layer_first":
                 transfer_kv_direct(
                     src_layers=self.packed_device_index_buffers,
